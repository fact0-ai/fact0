---
description: Show local Fact0 collector status and pending captures
allowed-tools: Bash
---
Run `"${CLAUDE_PLUGIN_ROOT}/bin/fact0-cc" status` and summarize the output. Explain any pending or failed captures. Governance and enforcement are disabled in this release. Do not print API keys or inspect raw queue payloads unless explicitly asked.
